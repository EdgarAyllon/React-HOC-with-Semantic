import { PureComponent } from 'react'
import withConnectionStatus from '../HOC/withConnectionStatus'

class Notifications extends PureComponent{
    render(){
        return null
    }
}
export default withConnectionStatus( Notifications )